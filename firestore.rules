
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Regla global que se aplica a todas las colecciones.
    // Permite a cualquier usuario autenticado leer y escribir datos.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
